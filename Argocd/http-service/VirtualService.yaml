apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: frontend-vs

spec:

  hosts:
    - boutique.quanldl.click

  http:

    - name: primary-route

      timeout: 5s

      retries:
        attempts: 3
        perTryTimeout: 2s

        retryOn: >
          gateway-error,
          connect-failure,
          refused-stream,
          unavailable,
          reset

      route:

        - destination:
            host: frontend
            subset: stable
          weight: 100

        - destination:
            host: frontend
            subset: canary
          weight: 0